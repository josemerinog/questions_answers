<%= form_for( [@question, @answer] ) do |f| %>
<%= render 'common/form_errors', object: @answer %>
	<p>
		<%= f.label :body, "Respuesta: " %>
		<%= f.text_field :body %>
		<%= f.submit "Publicar Respuesta" %>
	</p>

<% end %>